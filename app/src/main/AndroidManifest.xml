<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml — replace your existing file with this.
    Changes from original:
      • Added FOREGROUND_SERVICE permission (needed for background location in Android 13+)
      • Added POST_NOTIFICATIONS permission  (needed for push notifications in Android 13+)
      • No other changes — all existing activities are kept exactly as before.
-->
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Existing permissions -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <!-- Ongeza ndani ya <manifest> kabla ya <application> -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"
        tools:ignore="PhotoAndVideoPolicy,SelectedPhotoAccess" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- NEW — required for showing notifications on Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- NEW — required for background location sharing (driver tracking) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Storage permissions -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_ambulance_foreground"
        android:label="Ambulance"
        android:roundIcon="@mipmap/ic_ambulance_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.MySignupApp">
        <!-- FCM Service -->
        <service
            android:name=".AmbulanceMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- FCM default notification channel -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="ambulance_fcm" />

        <!-- Google Maps API key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBTbhvmNJnZT4HhivOR990rlmYvZ3YKCT4" />

        <!-- ─── Activities ──────────────────────────────────────────────── -->

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Home"
            android:exported="false" />

        <activity
            android:name=".Register"
            android:exported="false" />

        <activity
            android:name=".Register_admin"
            android:exported="false" />

        <activity
            android:name=".Drv_Home"
            android:exported="false" />

        <activity
            android:name=".Admin_Home"
            android:exported="false" />

        <activity
            android:name=".trial"
            android:exported="false" />

    </application>

</manifest>