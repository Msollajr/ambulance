<!-- ═══════════════════════════════════════════
     FILE 1: admin_request_row.xml
     Save as: res/layout/admin_request_row.xml
     ═══════════════════════════════════════════ -->
<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="12dp"
    android:layout_marginEnd="12dp"
    android:layout_marginTop="5dp"
    android:layout_marginBottom="5dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="3dp"
    app:cardBackgroundColor="#FFFFFF">

    <LinearLayout android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical" android:padding="14dp">

        <!-- Top row: severity + status + time -->
        <LinearLayout android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="8dp">

            <TextView android:id="@+id/tv_req_severity_adm"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:text="HIGH" android:textSize="11sp" android:textStyle="bold"
                android:textColor="#FFFFFF" android:paddingStart="8dp"
                android:paddingEnd="8dp" android:paddingTop="3dp" android:paddingBottom="3dp"
                android:background="@drawable/badge_navy" android:layout_marginEnd="8dp" />

            <TextView android:id="@+id/tv_req_status_adm"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:text="EN ROUTE" android:textSize="10sp" android:textStyle="bold"
                android:textColor="#FFFFFF" android:paddingStart="8dp"
                android:paddingEnd="8dp" android:paddingTop="3dp" android:paddingBottom="3dp"
                android:background="@drawable/badge_navy" />

            <View android:layout_width="0dp" android:layout_height="0dp"
                android:layout_weight="1" />

            <TextView android:id="@+id/tv_req_time"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:text="—" android:textSize="11sp" android:textColor="#888888" />
        </LinearLayout>

        <!-- Patient + Driver -->
        <TextView android:id="@+id/tv_req_user"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="Patient: —" android:textSize="13sp"
            android:textStyle="bold" android:textColor="#1A1A2E"
            android:layout_marginBottom="3dp" />

        <TextView android:id="@+id/tv_req_driver"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="Driver: — · Plate: —" android:textSize="12sp"
            android:textColor="#3B608C" android:layout_marginBottom="6dp" />

        <!-- Description -->
        <TextView android:id="@+id/tv_req_desc_adm"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="" android:textSize="12sp" android:textColor="#555555"
            android:layout_marginBottom="10dp" android:visibility="gone" />

        <!-- Track button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_track_request"
            android:layout_width="match_parent"
            android:layout_height="42dp"
            android:text="📍  Track on Map"
            android:textSize="13sp" android:textStyle="bold"
            android:textColor="#FFFFFF"
            app:backgroundTint="#3B608C"
            app:cornerRadius="10dp" />
    </LinearLayout>
</androidx.cardview.widget.CardView>
