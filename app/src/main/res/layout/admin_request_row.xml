<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="12dp"
    android:layout_marginEnd="12dp"
    android:layout_marginTop="5dp"
    android:layout_marginBottom="5dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="3dp"
    app:cardBackgroundColor="#FFFFFF">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="14dp">

        <!-- Top row: severity + status + time -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="8dp">

            <TextView android:id="@+id/tv_req_severity_adm"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:text="HIGH" android:textSize="11sp" android:textStyle="bold"
                android:textColor="#FFFFFF" android:paddingStart="8dp" android:paddingEnd="8dp"
                android:paddingTop="3dp" android:paddingBottom="3dp"
                android:background="@drawable/badge_navy" android:layout_marginEnd="8dp" />

            <TextView android:id="@+id/tv_req_status_adm"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:text="SEARCHING" android:textSize="10sp" android:textStyle="bold"
                android:textColor="#FFFFFF" android:paddingStart="8dp" android:paddingEnd="8dp"
                android:paddingTop="3dp" android:paddingBottom="3dp"
                android:background="@drawable/badge_navy" />

            <View android:layout_width="0dp" android:layout_height="0dp"
                android:layout_weight="1" />

            <TextView android:id="@+id/tv_req_time"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:text="—" android:textSize="11sp" android:textColor="#888888" />
        </LinearLayout>

        <!-- Patient name + phone -->
        <TextView android:id="@+id/tv_req_user"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:text="Patient: —" android:textSize="14sp"
            android:textStyle="bold" android:textColor="#1A1A2E"
            android:layout_marginBottom="3dp" />

        <!-- Driver info -->
        <TextView android:id="@+id/tv_req_driver"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:text="Driver: Not assigned" android:textSize="12sp"
            android:textColor="#3B608C" android:layout_marginBottom="6dp" />

        <!-- Description -->
        <TextView android:id="@+id/tv_req_desc_adm"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:textSize="12sp" android:textColor="#555555"
            android:layout_marginBottom="6dp" android:visibility="gone" />

        <!-- Insurance info — shown only if hasInsurance = true -->
        <TextView android:id="@+id/tv_req_insurance"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:textSize="12sp" android:textColor="#3B608C"
            android:background="#EEF4FF" android:padding="8dp"
            android:layout_marginBottom="8dp"
            android:visibility="gone" />

        <!-- Patient photo — shown if photoUrl exists -->
        <ImageView android:id="@+id/iv_req_photo_admin"
            android:layout_width="match_parent" android:layout_height="160dp"
            android:scaleType="centerCrop"
            android:background="#F7F8FA"
            android:layout_marginBottom="10dp"
            android:visibility="gone" />

        <!-- Button row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <!-- Assign Driver -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_assign_driver"
                android:layout_width="0dp" android:layout_height="42dp"
                android:layout_weight="1" android:layout_marginEnd="6dp"
                android:text="👤  Assign Driver"
                android:textSize="12sp" android:textStyle="bold" android:textColor="#FFFFFF"
                app:backgroundTint="#EA6D35" app:cornerRadius="10dp"
                android:visibility="gone" />

            <!-- Track on Map -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_track_request"
                android:layout_width="0dp" android:layout_height="42dp"
                android:layout_weight="1"
                android:text="📍  Track"
                android:textSize="12sp" android:textStyle="bold" android:textColor="#FFFFFF"
                app:backgroundTint="#3B608C" app:cornerRadius="10dp" />
        </LinearLayout>
    </LinearLayout>
</androidx.cardview.widget.CardView>